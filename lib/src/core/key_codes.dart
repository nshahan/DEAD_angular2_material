library angular2_material.src.core.key_codes;

// Can't use an enum because Dart doesn't support enum initializers.
class KeyCodes {
  static const ESCAPE = 27;
  static const SPACE = 32;
  static const UP = 38;
  static const DOWN = 40;
  const KeyCodes();
}
